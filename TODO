Cleanups:
     [ ] Make sure there is nothing left for TOTP.

CipherIgnore Templates:
     [ ] This is the same as .gitinore templates. Get as many as possible for multiple languages and frameworks. This will be used to ignore files that are not relevant to the project, such as build artifacts, logs, and temporary files.
     [ ] Create a .cipherignore file that can be used to specify patterns for files and directories to ignore when performing operations like encryption and decryption. This will help keep the project organized and prevent unnecessary files from being processed. 

CLI Commands:
     [ ] Verify if this command is useful or remove: cipherlock <src> <key_file> <dest>   Run one-off 'Cold' encryption.
     [ ] Verify if the directory provided in the boot command is a valid location else display an error. 
     [ ] Remove the `setup` command as it is not needed. The `boot` command can handle both interactive and one-off encryption modes.
     [ ] Recheck the cold mode. It might be redundant if the boot command can handle one-off encryption. If it is redundant, remove the cold mode to simplify the CLI and reduce confusion for users.
     [ ] The decrypt and share functions are available. Include them as commands in the cli.
     [ ] Add command in hot mode, maybe in settings to edit current profile and to switch, create profiles. Even delete.
     [ ] In cipherlock status, the print out should be labeled properly. Project description should be after project name.
     [ ] Reverify the project backup key feature

Documentation:
     [ ] Create a comprehensive user guide that covers all features of the tool, including how to use the CLI commands, manage user profiles, and customize themes. This guide should be easy to understand and include examples to help users get started quickly.
     [ ] Create a troubleshooting section in the documentation to help users resolve common issues they may encounter while using the tool. This could include solutions for common error messages, tips for optimizing performance, and guidance on how to securely manage encryption keys.

Feature - Encryption:
     [ ] Allow users rename and relocate the primary key file. This would provide more flexibility in how users manage their encryption keys and could enhance security by allowing them to store the key file in a more secure location or under a different name to make it less obvious.
     [ ] Implementing support for multiple encryption algorithms, allowing users to choose the one that best suits their needs. 
     [ ] Test this claim: Self-Healing Recovery: If the local master.clkey is deleted, CipherLock will automatically attempt to restore it from the profile backup during the boot process.
     [ ] Improve or remove the encryptor animation.
     [ ] Implementing a feature to allow users to specify a custom header for their encrypted files. This header could include metadata about the encryption, such as the algorithm used, the date of encryption, or other relevant information. This would help users keep track of their encrypted files and provide additional context when working with them.  

Feature: Encrypted File Header:
     [ ] Let users be able to provide the custom location of their header file, the default would be in the .cipherlock folder.

Feature - Project management:
     [ ] Should there be a feature where project metadata history is stored and can be accessed? This would allow users to track changes to project information over time and could be useful for auditing purposes or for understanding the evolution of a project.

Feature - Project programming language detection:
     [ ] Implementing a feature to automatically detect the programming language of a project based on its file structure and contents. This could be useful for providing language-specific features, such as syntax highlighting, code snippets, or language-specific templates.
     [ ] Detect text files, pdfs, word documents etc

Feature - User Profiles:
     [ ] Allow feature to check the total number of backed up keys for projects a profile has.
     [ ] Add feature to edit profile when in a hot mode for a project.
     
Generated Files:
     [ ] Implementing a feature to automatically generate files with specific content or structure based on user input. This could be useful for creating configuration files, templates, or other types of files that are commonly used in development projects.

Localization:
     [ ] Implementing localization support to allow users to use the tool in their preferred language. This could involve creating a system for managing translations and allowing users to switch between languages easily.
     [ ] French translations need help. It is AI generated.
     [ ] Check the vault_loaded and no_vault_at_path translations. They are only in the english translation file and not in use in the project. Go figure.

Themes:
     [ ] Implementing a theming system to allow users to customize the appearance of the tool. This could include options for different color schemes, font styles, and other visual elements to enhance the user experience.
     [ ] Consider localizing the name of themes.

User profiles:
     [ ] Implementing user profiles to allow multiple users to have their own settings and preferences within the tool. This could include things like preferred encryption algorithms, default file paths, and other customizable options.
     [ ] Should user profiles be encrypted? If so, we need to decide on a secure method for storing and retrieving user profile information.
     [ ] User profiles should be stored in a secure location, such as an encrypted file or a secure database, to protect sensitive information. Consider implementing encryption for user profiles to enhance security.

Vault:
     [ ] PBKDF2 Iterations: You are using 10,000 iterations. In 2026, this is considered low for high-security applications. Recommendation: Increase this to at least 600,000 iterations or switch to Argon2id.








